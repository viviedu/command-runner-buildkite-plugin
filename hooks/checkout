#!/bin/bash
set -euo pipefail

echo "--- running checkout command"

command="${BUILDKITE_PLUGIN_COMMAND_RUNNER_CHECKOUT:-}"

if [ -z "$command" ]; then
  echo "No command specified. Skipping."
else
  echo "Running command: $command"
  eval "$command"
fi

echo "--- finished running checkout command"