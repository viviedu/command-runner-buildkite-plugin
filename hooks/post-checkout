#!/bin/bash
set -euo pipefail

echo "--- running command"

command="${BUILDKITE_PLUGIN_COMMAND_RUNNER_COMMAND:-}"

if [ -z "$command" ]; then
  echo "No command specified. Skipping."
else
  echo "Running command: $command"
  eval "$command"
fi

echo "--- finished running command"